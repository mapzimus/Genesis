# Project Genesis Instructions

The operating instruction set for every session in this project lives in `CLAUDE.md`, which Claude Code loads automatically. Read and follow `CLAUDE.md`; this file is a pointer kept for tools that look for `AGENTS.md`.

Protocol v1.0.0 kept the full instruction set in this file under the Codex platform; it moved to `CLAUDE.md` in the v2.0.0 Claude migration (see `PREREGISTRATION.md` Amendment record). The historical text is preserved in Git history and `snapshots/`.
