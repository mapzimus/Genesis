# Day 1 Readiness Gate

Day 1 is blocked until every box below is complete. Completing the workspace and tests does not start the experiment.

- [ ] Merge the v2.0.0 migration to `main`, then record the amendment's public timestamp in `PREREGISTRATION.md`.
- [ ] A fresh Claude Code cloud session created from this repository loads `CLAUDE.md` automatically and reports the fixed parameters (replaces the v1 trusted-Codex-task item).
- [ ] A cloud session demonstrates the push discipline: a records commit lands on `main` from inside a cycle (replaces the v1 keep-awake item).
- [ ] Dedicated Genesis email exists and authentication works.
- [ ] A legally usable payment method is verified and can be revoked or disabled.
- [x] Free preview/static hosting works through GitHub Pages.
- [ ] Exactly $115 is separated in the accounting envelope.
- [x] A safe public path exists at `https://mapzimus.github.io/Genesis/`.
- [x] Preregistration and frozen evaluator are published and timestamped in the public repository (v1.0.0 on 2026-08-08; v2.0.0 pending merge, first box above).
- [ ] Both Routines are created per `AUTOMATIONS.md` and their IDs recorded in `READINESS_EVIDENCE.md`.
- [ ] Operator cycle has completed a no-write manual dry run in the cloud environment.
- [ ] Close cycle has completed a no-write manual dry run in the cloud environment.
- [x] The automated acceptance suite passes (17/17 on 2026-08-12).
- [x] A separate read-only task reconstructs the readiness state from repository files only (PASS on 2026-08-08; re-verified post-migration on 2026-08-12).

When all boxes are complete, record the evidence without credentials, calculate the three dates from the selected Day 1, update `STATE.json`, and only then activate the frozen goal and the Routines.

Never add a product idea, market recommendation, customer list, product name, domain, marketing copy, or strategic hint during readiness.
