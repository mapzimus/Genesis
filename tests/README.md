# Acceptance suite

These tests exercise the protocol's accounting, earned-revenue gates, source exclusions, approval enforcement, action idempotency, lock overlap, stale-lock recovery, dashboard privacy scan, evaluator freeze, and readiness gate.

They use temporary copies and never modify the live ledger.
