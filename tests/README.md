# Acceptance suite

These tests exercise the protocol's accounting, earned-revenue gates, source exclusions, approval enforcement, action idempotency, lock overlap, stale-lock recovery, dashboard privacy scan, evaluator freeze, readiness gate, and pre-lock cycle blocking.

They use temporary copies and never modify the live ledger.
